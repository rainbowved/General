# 2P_REVIEW_LOG

| date | proposal | counterargument | decision | tests | rollback_plan | owner_phase |
|---|---|---|---|---|---|---|
| 2026-03-01 | Bootstrap PHASE 0 shell CI/check_sins only. | Risk of false positives in pattern scans. | Keep scan minimal; fail-closed only on explicit banned tokens. | `tools/check_sins.sh` + NEG-P0 injection. | Replace with C# CheckSins in PHASE 1. | PHASE 0 |

| 2026-03-01 | Add Phase 2 validate/coverage gates with golden compare. | Risk of overfitting fixtures and weak anchor coverage. | Enforce >=30 ledger rules, anchor gate, and 10 negative fixtures. | `tools/validate_pack.sh --seed/--neg`, `tools/coverage_report.py`. | Tighten rule corpus in P3 if drift appears. | PHASE 2 |
| 2026-03-01 | Add PHASE 3 deterministic build_pack + repro + golden build fixtures. | Risk: sqlite writes may vary if insert order unstable. | Enforce sorted insert order and compare DB sha/report with golden. | `tools/build_pack.sh`, `tools/golden_compare.sh`, CI full P3. | If unstable, freeze SQLite pragmas and order-by queries. | PHASE 3 |
| 2026-03-01 | Add PHASE 4 seed discipline + controlled golden update policy. | Risk: accidental golden drift can mask regressions. | Require explicit accept flag via `tools/update_golden.sh`; log each update in `_logs/update_golden.log`. | `tools/update_golden.sh --accept-golden`, full CI P4. | If policy fails, block golden writes and revert fixture changes. | PHASE 4 |
| 2026-03-01 | Add PHASE 5 client MVP smoke loop with LOOT_OPEN-only grants. | Risk: client outputs may drift if order/hash not canonical. | Canonicalize summary + compare to golden smoke output in CI. | `client/run_smoke.sh`, `CI_LEVEL=full PHASE_TARGET=5`. | Freeze script order and serialization; regenerate golden only via reviewed change. | PHASE 5 |
| 2026-03-01 | Add PHASE 6 determinism harness with golden trace hash. | Risk: hidden nondeterminism may pass single-run smoke. | Run script twice, compare full summaries, and assert expected state hash. | `client/run_determinism.sh`, `CI_LEVEL=full PHASE_TARGET=6`. | Freeze script + RNG key contract, regenerate expected hash only via reviewed update. | PHASE 6 |
| 2026-03-01 | Add PHASE 7 deterministic narrative picks for POI/archive flows. | Risk: flavor text randomness can break determinism. | Use keyed RNG with scope_id=event_id and static variant arrays. | `client/run_smoke.sh`, `client/run_determinism.sh`, `CI_LEVEL=full PHASE_TARGET=7`. | Revert narrative file or lock variant order if hashes drift. | PHASE 7 |
| 2026-03-01 | Harden PHASE 7 with explicit narrative integrity gate in CI. | Risk: narrative field could disappear while smoke still passes structurally. | Add `narrative_check` step and require it for PHASE_TARGET>=7. | `client/run_narrative_check.sh`, `CI_LEVEL=full PHASE_TARGET=7`. | If noisy, demote to optional and keep deterministic hash checks. | PHASE 7 |
| 2026-03-01 | Add Python CI driver (`tools/py/ci.py`) and make wrappers delegate to it. | Risk: divergence between shell/ps1 logic and Python stack spec. | Single source of CI truth in Python; wrappers become thin launchers. | `CI_LEVEL=full PHASE_TARGET=7 bash tools/ci.sh`, `python3 tools/py/ci.py`. | Restore previous wrappers if python driver unavailable. | PHASE 7 |
